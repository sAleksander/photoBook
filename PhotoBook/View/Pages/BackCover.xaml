<UserControl x:Class="PhotoBook.View.Pages.BackCover"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PhotoBook.View"
      DataContext="{Binding Source={StaticResource Locator}, Path=BackCover, Mode=OneTime}"
      mc:Ignorable="d" 
      d:DesignHeight="1500" d:DesignWidth="800">

    <Grid>
        <Viewbox>
            <Canvas Name="BookCanvas"/>
        </Viewbox>
    </Grid>
    
    <!--
    <Canvas Grid.Column="0" Background="White">
        <Image Source="../Images/logoBlack.png" 
                Width="100" Canvas.Top="1061" Canvas.Left="355" Height="100"/>
        <TextBlock Text="Wykonali: Katarzyna Pawlus, Wojciech Pisarski, Aleksander Szulc, Bartłomiej Zięba" 
                    Canvas.Left="170" Canvas.Top="1185"
                    FontFamily="Comic Sans MS"/>
    </Canvas>
    -->
</UserControl>
